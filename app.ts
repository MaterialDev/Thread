/// <reference path="typings/angularjs/angular.d.ts" />

module thread {
    "use strict";

    angular.module('thread', [
        'thread.scrollCollapse',
        'thread.waveEffect',
        'thread.menu'
    ]);
}